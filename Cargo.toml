[package]
name = "mcp-embedded-ui"
version = "0.5.0"
edition = "2021"
description = "A lightweight embedded Web UI for any MCP Server"
license = "Apache-2.0"
readme = "README.md"
authors = ["aiperceivable <tercel.yi@gmail.com>"]
homepage = "https://aiperceivable.github.io/mcp-embedded-ui/"
repository = "https://github.com/aiperceivable/mcp-embedded-ui-rust"
documentation = "https://github.com/aiperceivable/mcp-embedded-ui"
keywords = ["mcp", "ui", "explorer", "tools"]
categories = [
    "web-programming",
    "web-programming::http-server",
    "api-bindings",
    "development-tools",
]

[package.metadata.docs.rs]
all-features = true
rustdoc-args = ["--cfg", "docsrs"]

[dependencies]
axum = "0.8"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
async-trait = "0.1"
tracing = "0.1"
tokio = { version = "1", features = ["full"] }
tower = "0.5"
jsonschema = { version = "0.46.2", default-features = false }

[dev-dependencies]
async-trait = "0.1"
tokio-test = "0.4"
tower = { version = "0.5", features = ["util"] }
hyper = "1"
http-body-util = "0.1"
tracing-subscriber = "0.3"
